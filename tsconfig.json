{
    "compilerOptions": {
        "target": "es5", // compile to es5 since to build a package with browser compatibility.
        "lib": ["dom", "es7"],
        "strict": true
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "dist", "cypress", "src/**/*.test.ts"]
}
