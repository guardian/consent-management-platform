// describe('onConsentChange', () => {
// 	it('invokes callbacks regardless of when the cmp is initialised', () => {});
// 	it('invokes a new callback immediately if consent state is known', () => {});
// 	it('only invokes callbacks if the consent state has changed', () => {});
// 	it('invokes callbacks with the relevant privacy framework', () => {});
// });
